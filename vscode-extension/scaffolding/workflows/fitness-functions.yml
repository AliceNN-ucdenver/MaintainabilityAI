name: "Fitness Functions"

on:
  pull_request:
  push:
    branches: [main]

jobs:
  fitness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
{{SETUP_STEPS}}
      - run: {{INSTALL_CMD}}
      - run: {{TEST_COVERAGE_CMD}}
      - run: {{TEST_FITNESS_CMD}}
        continue-on-error: true  # WARNING MODE — remove after 2 weeks of baseline
